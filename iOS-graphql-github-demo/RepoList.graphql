query RepoList($first:Int){
  search(query: "repository:graphql", type: REPOSITORY, first: $first) {
    repositoryCount
    nodes {
      ... on Repository {
        name
        owner {
          login
          avatarUrl
        }
        stargazerCount
      }
    }
  }
}
